// import express from 'express';
// import * as ProfileController  from '../../controllers/profileController.js'
// const router = express.Router()
// import {passwordUpdate, profileUpdate, userChallengeStatistics} from '../../controllers/profileController.js';
// import auth from '../../middlewares/auth.js';
// import cors from 'cors';

// router.use(cors({
//   credentials : true
// }));

// router.get('/', auth, ProfileController.userProfile);
// router.patch('/', auth, ProfileController.profileUpdate);

// router.get('/history', auth, ProfileController.userChallengeStatistics);
// router.patch('/password', auth, ProfileController.passwordUpdate);
// router.patch('/phone', auth, ProfileController.profileUpdate);
// router.patch('/email', auth, ProfileController.emailUpdate);
// // coupon activation router
// router.post('/coupon', auth, ProfileController.couponActivation);

// export default router
